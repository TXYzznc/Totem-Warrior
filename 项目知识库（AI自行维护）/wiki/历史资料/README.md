# 历史资料说明

本目录保存已经被处理和索引过的旧资料。它们有参考价值，但不代表当前项目事实。

## 使用规则

- 当前事实优先级：用户最新口头确认 > 当前 GF_X runtime 实现 > active openspec > 历史资料。
- 看到旧 `Assets/Scripts`、`GameApp`、`ModuleRunner`、`EventBus`、旧 `InputModule`、旧 `DataTableModule` 时，只能当迁移前证据。
- GDD v2.1 和初版 GDD 可以用于理解需求来源，但不能直接决定当前实现。
- 如果历史资料和当前 `PROJECT_MAP.md`、`ACTIVE_CONTEXT.md`、`manifests/*.json` 冲突，以当前索引和代码为准。

## 当前内容

| 目录 | 状态 | 说明 |
|---|---|---|
| `GDD-v2/` | historical-reference | 早期完整 GDD，已被后续口头确认和 GF_X 重构部分修正 |
| `初版GDD-2026-06/` | historical-reference | 立项阶段原始草案，已完成整理和降权 |
