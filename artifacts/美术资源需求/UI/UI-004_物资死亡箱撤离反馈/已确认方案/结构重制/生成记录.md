# UI-004 结构重制素材生成记录

日期：2026-07-27  
范围：`UI-004_物资死亡箱撤离反馈/已确认方案/结构重制/`

## 视觉输入与边界

- 已查看工作区内的 `UI-004_模块玩具_物资死亡箱撤离反馈_全状态_B.png`、`UI-004_颜料失序_物资死亡箱撤离反馈_全状态_C.png`，以及旧目录中的撤离锚点与范围环参考图。
- 素材清单指定的 `UI-004_清透机能_物资死亡箱撤离反馈_全状态_A.png` 不在当前工作区；本次没有将 B/C 的 mockup 裁作最终资源。
- 未生成物品、掉落物、物资箱、头像、身份牌、事件图、补给图、数值、文本、键位或状态文案。

## 带材质组件

- 工具：内置 `image_gen`，生成后使用 `remove_chroma_key.py` 处理。
- 原始内置生成输出：`_source/ui004_semantic_shells_raw_imagegen.png`；为保留模型原始结果而不覆盖。
- 标准化绿幕源：`_source/ui004_semantic_shells_green.png`；仅将背景的绿幕像素归一为严格 `#00ff00`，并抽样断言五个空白背景点均为该值。
- 整图去绿：`_merged/ui004_semantic_shells_alpha_normalized.png`；去绿器实测键色为 `#00ff00`。同时保留首次去绿的 `_merged/ui004_semantic_shells_alpha.png` 以便追溯；未从效果图/mockup 中裁取元素。
- 从整图去绿结果按独立网格单元切出：
  - `slot_loot_rarity_frame.png`
  - `badge_loot_property_persistent.png`
  - `badge_loot_property_drop.png`
  - `frame_deathbox_identity.png`
  - `anchor_extract_gate.png`

## 纯色几何组件

以下资源以 PIL 在透明 RGBA 画布绘制，不含材质、高光或烘焙内容：

| 文件 | 画布 | 参数摘要 |
| --- | ---: | --- |
| `ring_extract_out_of_range.png` | 512×512 | 双层分段圆弧、四个菱形端点；琥珀/灰蓝，运行时驱动动画。 |
| `timeline_late_event_track.png` | 1024×192 | 可拉伸轨道、4 个中性菱形节点容器、无阶段/数字。 |
| `callout_high_value_supply.png` | 768×320 | 角切空呼出框、连接线和空 runtime-slot，12px 切片安全边距。 |

## 通用组件复用

`panel_loot_evac_section` 与 `overlay_loot_evac_feedback` 没有重复导出 PNG：前者引用通用 `PanelFrame`，后者引用 `PanelFrame` 与通用 `state_*`。其文字、图标和输入提示保持 TMP/运行时节点。
