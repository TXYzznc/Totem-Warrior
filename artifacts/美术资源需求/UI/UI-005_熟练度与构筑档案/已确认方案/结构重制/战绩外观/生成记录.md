# UI-005 战绩与外观展示：新版专属壳体生成记录

- 日期：2026-07-27
- 范围：仅 `ProfileShowcaseForm` 的新版专属静态 `ui-shell`；没有移动、删除、重命名或覆盖旧 `战绩外观` 目录内的任何文件。
- 依据：`素材清单.md`、`openspec/changes/restructure-ui-asset-catalog/art/prefab-layout.md`、`artifacts/美术资源需求/通用UI组件/组件规范.md`。

## 产出

1. `profile_showcase_icon_appearance.png`：外观预设分区的无文字抽象层级标记。
2. `profile_showcase_empty_mark.png`：空预设 / 空徽章状态共用的中性档案标记。

## 方法与可追溯性

- 两项均为纯色几何，使用 Pillow 以 4x 超采样、Lanczos 下采样直接生成透明 RGBA PNG；不含材质、照片或复杂边缘，因此不适用 `#00ff00` 绿幕、去绿或切图流程。
- 源脚本：`_source/generate_profile_showcase_shells.py`；配方：`_source/geometry_recipe.json`；验证脚本：`_source/validate_profile_showcase_shells.py`。
- `_merged/profile_showcase_shells_merged.png` 是透明底的可追溯排列，不是 UI mockup，也不是从 mockup 裁切。`_merged/` 中的单项文件仅作中间留档；运行时只能引用 `最终素材/`。

## 未生成内容

- 未生成角色外观、服装、纹身、预设内容/缩略图、八图案徽章、头像、分享预览、战绩数值、日期、百分比、输入 glyph 或任何文字。
- 未生成任何通用面板、卡片、焦点/选择、进度、状态、输入提示壳体；这些均依赖通用 UI 组件。
- 旧版三种战绩类别图标仅保留为迁移候选，本轮没有复制或修改；详见 `素材映射.md`。
