# UI-005 新解锁反馈｜生成记录

生成日期：2026-07-27  
生产范围：仅 `unlock_feedback_tier_badge_frame`（1 项）。

## 产物

| 最终 Sprite | 尺寸 | 分类 | Unity 用法 |
| --- | ---: | --- | --- |
| `最终素材/unlock_feedback_tier_badge_frame.png` | 512×512 | `ui-shell` | `Image Simple`；用于 `MasteryMilestone`、`BeforeProgress`、`AfterProgress` 的无文字六边空章。 |

## 制作方法与可追溯性

- 该资源是纯色几何轮廓，不含材质、照片、纹理或生成式绘制，因此按组件规范采用 PIL 透明 RGBA 流程；**未调用 image_gen，也不需要绿幕去背**。
- 可复现脚本：`_source/build_unlock_feedback_tier_badge.py`。
- 几何与语义配方：`_source/geometry_recipe.json`。
- 非运行时中间合成：`_merged/unlock_feedback_tier_badge_frame_composite.png`；32px 可读性复核：`_merged/unlock_feedback_tier_badge_frame_32px_review.png`。
- 图形为三层六边形框、三个无语义结构锚点和透明中心。中心不含固定图案、等级、进度、奖励、文字或成功标记。

## 边界确认

- 模态、卡片、按钮、焦点、锁、进度、输入提示及 loading/success/failure 均未生成，按 `通用UI组件/组件规范.md` 在运行时引用。
- 解锁图案、样式内容、奖励物、前后进度数值与所有文字均保留给运行时资源或 TMP。
- 未移动、删除、重命名或覆盖旧 `已确认方案/解锁反馈/` 中的任何文件。
