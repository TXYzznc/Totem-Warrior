# UI-005 熟练总览｜生成记录

生成日期：2026-07-27

本次只生产“结构重制/熟练总览”中无法由通用组件库复用的 3 张页面语义 Sprite。已核对全局 `prefab-layout.md` 与 `通用UI组件/组件规范.md`：`panel_frame`、`card_content`、`focus_outline`、`selection_frame`、`ring_*`、`prompt_base`、`state_*` 均已有通用来源，不制作副本。

## 产物

| 最终文件 | 尺寸 | 用途 | 方式 |
| --- | ---: | --- | --- |
| `最终素材/mastery_overview_header_mark.png` | 256×256 | 页眉档案标签 | PIL 纯几何、4×超采样后 Lanczos 缩放 |
| `最终素材/mastery_overview_header_divider.png` | 512×48 | 页眉左右细分隔 | PIL 纯几何、4×超采样后 Lanczos 缩放 |
| `最终素材/mastery_overview_empty_mark.png` | 256×256 | 空档案/同步降级标识 | PIL 纯几何、4×超采样后 Lanczos 缩放 |

可追溯源：`_source/build_mastery_overview_assets.py` 与 `_source/geometry_recipe.json`。合并核图仅位于 `_merged/mastery_overview_geometry_merged.png`，不是运行时素材，也不是页面 mockup。

## 生产边界

- 三张图均为纯色线框与半透明几何，不含材质、描边高光贴图或纹理；因此按规范使用 PIL 透明 RGBA 流程，未调用 image_gen，也没有绿幕去除步骤。
- 未生成八图案图形、拥有/未拥有/锁定状态位图、熟练度数字、头像、成就徽章、导航/奖励图标、输入 glyph 或任意文字贴图。
- 初始拥有与未拥有由 `PatternMasteryState` 在运行时决定；锁定和其他语义状态叠加通用 `state_`，不增加页面专属副本。
- 未裁切效果图或 mockup，未删除、移动或改写旧 `熟练总览` 目录。
