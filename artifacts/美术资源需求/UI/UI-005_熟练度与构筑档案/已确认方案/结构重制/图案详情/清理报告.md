# UI-005｜图案详情旧目录清理报告

日期：2026-07-27  
授权范围：仅清理 `已确认方案/图案详情/` **根目录**中《素材清单.md》审计表标记为“删除候选”的最终 PNG。

## 执行结果

- 删除：16 个根目录最终 PNG。
- 保留：3 个根目录最终 PNG（均为新版静态 Sprite 保留候选）。
- 未删除或移动：`_merged/`、`_cut/`、`生成记录.md`、绿幕/切图审计文件、`结构重制/`；根目录原本不存在 `_source/`，未对其执行任何操作。
- 删除前逐项验证：16 个目标均为本根目录中的普通文件，且均不在保留候选列表中。
- 删除后验证：16 个删除目标均不存在；3 个保留目标均存在；根目录仅余 3 个 PNG。

## 已删除（16）

| 文件 | 台账删除依据 |
| --- | --- |
| `detail_badge_check.png` | 归并为通用 `state_success`。 |
| `detail_badge_lock.png` | 归并为通用 `state_lock`。 |
| `detail_currency_crystal.png` | 货币图标由经济数据/运行时资产目录提供。 |
| `detail_nav_normal_frame.png` | 归并为通用 `ContentCard/ListRow`。 |
| `detail_nav_selected_triangle.png` | 混入动态图案；焦点壳通用、图案运行时加载。 |
| `detail_pattern_triangle_primary.png` | 主图案由 `PatternDefinition.primaryGraphic` 运行时提供。 |
| `detail_progress_fill_cyan.png` | 归并为通用 ProgressBar 填充。 |
| `detail_progress_track_empty.png` | 归并为通用 ProgressBar 轨道。 |
| `style_frame_locked.png` | 使用通用 ContentCard + `state_lock`。 |
| `style_frame_unlocked.png` | 使用通用 ContentCard，状态由数据驱动。 |
| `style_triangle_classic.png` | 样式预览为 `StyleDefinition.preview` 运行时内容。 |
| `style_triangle_dotwork.png` | 样式预览为 `StyleDefinition.preview` 运行时内容。 |
| `style_triangle_etched.png` | 样式预览为 `StyleDefinition.preview` 运行时内容。 |
| `style_triangle_interlaced.png` | 样式预览为 `StyleDefinition.preview` 运行时内容。 |
| `style_triangle_ornamental.png` | 样式预览为 `StyleDefinition.preview` 运行时内容。 |
| `style_triangle_prismatic.png` | 样式预览为 `StyleDefinition.preview` 运行时内容。 |

## 已保留（3）

| 文件 | 新版计划归属 | 删除后验证 |
| --- | --- | --- |
| `detail_connector_node.png` | `pattern_detail_permission_connector_node` | 存在于旧目录根层。 |
| `detail_mastery_hex_frame.png` | `pattern_detail_mastery_badge_frame` | 存在于旧目录根层。 |
| `detail_tier_crest.png` | `pattern_detail_tier_crest` | 存在于旧目录根层。 |

## 保护项验证

| 项目 | 结果 |
| --- | --- |
| 根目录删除候选仍存在 | 0 / 16 |
| 根目录保留候选缺失 | 0 / 3 |
| 根目录剩余 PNG | 3（且仅为上述保留项） |
| `生成记录.md` | 存在 |
| `_merged/` 及其审计文件 | 存在（7 个文件） |
| `_cut/` 及其审计文件 | 存在（28 个文件） |
| `_source/` | 清理前根目录未包含该目录；未执行操作 |
| `结构重制/图案详情/素材清单.md` | 存在，未改动 |

本次仅对上表 16 个绝对路径文件执行非递归删除；没有把目录、文档、绿幕审计文件或新版结构目录作为删除目标。
