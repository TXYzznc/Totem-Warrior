# UI-002 纹身工作台 C_v2 — 生成记录

- 日期：2026-07-27
- 视觉依据：`../UI-002_颜料失序_纹身工作台_主界面_C_v2.png`（已人工查看；未裁切）。
- 结构契约：`openspec/changes/restructure-ui-asset-catalog/art/prefab-layout.md`。
- 通用组件约束：`artifacts/美术资源需求/通用UI组件/组件规范.md`。

## 产出范围

共交付 9 张透明 PNG：清单 §5 的 7 项专属壳体，及清单 §4 要求的两个预览叠层语义壳（empty、error）。预览 loading 继续引用通用 `state_loading`，因此没有制造页面重复版本。

未生成：角色模型、纹身贴花、颜料物品、样式/库存图案、Before/After 材质预览，以及任何面板、行、卡、按钮、焦点、进度、状态、输入提示或普通文字。

## 生成方式与可追溯文件

- 工具：Pillow（PIL）纯色几何绘制，4× 超采样后缩放为最终分辨率。
- 选择理由：所有输出均为无材质、无纹理、无高光、无文字的几何轮廓/符号；符合组件规范中“纯色几何可 PIL”的例外，故未触发 image_gen 绿幕抠图流程。
- 源脚本：`_source/generate_tattoo_ui_assets.py`。
- 参数记录：`_source/PIL参数.json`。
- 合并审阅图：`_merged/tattoo_workbench_dedicated_shells_contact_sheet.png`。
- 验证脚本：`_source/validate_tattoo_ui_assets.py`。

## 命名与运行时约束

所有名称不含状态文案、数值、百分比或输入键名。部位图标 normal/focused/selected 由 `Tint` 与通用 `SelectionFrame` 组合；错误详情、空态说明等文字由 TMP_Text 注入。`tattoo_workbench_empty_slot` 不含皮革、纹身、角色或任何库存内容。
