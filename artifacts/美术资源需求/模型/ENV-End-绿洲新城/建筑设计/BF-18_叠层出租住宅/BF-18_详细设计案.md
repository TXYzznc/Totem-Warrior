# BF-18 叠层出租住宅｜P1 详细设计案

本文与 JSON 互为唯一尺寸/位置真源。布局采用 X右/Y后/Z上，原点为首层完成面中心、正门 `-Y`；Unity 映射 `Unity(X,Y,Z)=Layout(X,Z,Y)`，单位为米。

## 结构、住户与连续楼梯

包围盒 `12×16×9m`，墙厚外/内 `0.30/0.20m`。L1 完成面 `0`、净高 `4.2`、板顶 `4.8`：F18-01 下层前室 `4.8×3.8`、F18-02 下层起居 `4.8×7.2`、F18-03 独立楼梯厅 `3×8.2`、F18-06 共用角 `3×3`。L2 完成面 `4.8`、净高 `3.6`、板顶 `9`：F18-04 到达平台 `3×2.4`、F18-05 上层起居 `8.2×8.4`。

D01 下层街门 `(-2.3,-8,1.3)`、D02 楼梯街门 `(3.7,-8,1.3)` 均与入口错开；D03 上层户门、D04 下层内门及 W01–W04 坐标/规格严格按 JSON。ST01 为独立 U 形双跑：净宽 `1.2m`，28 级×`0.1714286m`=`4.8m`，每跑14级，踏面 `0.30m`，中间平台 `2.7×1.2m` 于 `(3.7,0.9,2.4)`，头部净空≥`2.4m`，栏杆 `1.1m`；不得改为装饰外梯。

坐标索引（Layout）：D03/D04=`(2.2,-1.5,6.05)/(-2.3,-3.8,1.25)`；W01–W04=`(-4.6,-8,1.6)/(-2.3,8,1.6)/(-1,-8,6.4)/(-1,8,6.4)`；P01/P02/P03=`(-4.4,2.6,0)/(-2.8,2.6,4.8)/(4.6,4.5,0)`；A01/A02=`(-2.3,1.2,0)/(-1,1.2,4.8)`。

下层路线 `D01→F18-01→F18-02→D01`；上层路线 `D02→ST01→F18-04→D03→F18-05→ST01→D02`，上下住户绝不穿越彼此起居。P01 床、P02 桌、P03 洗衣点和 A01/A02 均按 JSON 落位，避让平台与门扇。接错层居住次巷，仅保留两处门前 `1.8m` 净空，不写世界位置；10 个实例可换楼梯侧、住户数/故事与窗节奏。

## 碰撞与验收

楼板、墙、栏杆、楼梯、家具为实体简化碰撞；锚点无实体碰撞。代理必须能无跳跃完成两条路线；验收层2、门4、窗4、楼梯1，核对总踏升闭合4.8m、两入口无冲突。
