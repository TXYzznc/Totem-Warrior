## 1. 现状基线与配置骨架

- [x] 1.1 审计 `PCGMapGenerator`、`TotemMapService`、锚点消费者和资源目录，记录固定模板、固定锚点与旧路径的调用图。
- [x] 1.2 定义 `PCGWorldPlan`、宏观单元、微观采样记录、事件锚点、视觉请求、能力记录和诊断快照的纯 C# 数据模型。
- [x] 1.3 定义版本化主题地貌、特征配方、能力激活、事件布局和视觉角色配置 DTO，并确定与现有项目数据管线的装载方式。
- [x] 1.4 建立三主题四地貌的初始配置，录入面积预算、形态配方、视觉角色、事件亲和性和未来能力元数据。
- [x] 1.5 建立基线事件布局配置，包含出生、Boss、商人、纹身师和宝箱的最低配额、最小距离和亲和性规则。

## 2. 确定性世界计划生成

- [x] 2.1 实现请求归一化、具名子种子派生和 World Plan 可重复哈希，禁止生成流程使用 Unity 全局随机数。
- [x] 2.2 实现宏观基底、区域、密度场和事件/视觉保留区的生成，使用预分配格网数据。
- [x] 2.3 实现通用 Blob、Ribbon、Chain、Scatter 和 Fringe 特征配方执行器及面积、间距、优先级约束。
- [x] 2.4 实现四地貌面积预算校正和失败诊断，确保有效配置不静默遗漏地貌。
- [x] 2.5 实现微观事件候选采样、加权选择、最小距离过滤、配额验证和明确失败诊断。
- [x] 2.6 实现按密度、地貌、保留区和净空区采样的静态视觉投放请求生成。
- [x] 2.7 实现能力元数据汇总及“仅视觉、全图可通行”的基线激活策略。

## 3. 运行时适配与资源迁移

- [x] 3.1 为 World Plan 编写到现有 Tilemap/地形渲染输入的适配器，不从渲染结果反推规则。
- [x] 3.2 为 `TotemMapService` 提供 World Plan 适配入口，并保持地图加载缓存和对象预算策略。
- [x] 3.3 将锚点视觉目录从精确 anchor ID 查询迁移为主题、事件类型和视觉角色候选查询。
- [x] 3.4 迁移已导入的主题静态物件与 41 项锚点视觉壳，验证其主题、地貌和语义角色标签。
- [x] 3.5 实现按事件类型查询多个锚点的 API，并提供旧单例锚点的主锚点兼容适配。
- [x] 3.6 将现有出生、Boss、NPC、宝箱和遭遇消费者迁移到事件类型/集合查询，记录仍需要的兼容点。

## 4. 废弃路径清理

- [ ] 4.1 移除 `BspMaxDepth` 的运行时、缓存和数据表读写路径，并保留必要的配置迁移说明。
- [x] 4.2 移除未消费的 `ZoneRuleCatalog` 加载、资源文件和关联死代码。
- [x] 4.3 移除地形边缘匹配、过渡遮罩和水边底图的未调用实现、配置与测试。
- [x] 4.4 将旧固定主题模板和固定房间中心锚点实现切换为 World Plan 后删除，并移除不再成立的单线水道假设。
- [x] 4.5 搜索并修复所有已删除概念的引用，确保编译和资源加载不依赖旧路径。

## 5. 验证、性能与交接

- [x] 5.1 为确定性子种子、World Plan 哈希、双尺度关联和全图可通行基线编写编辑器测试。
- [x] 5.2 为五种通用特征配方、四地貌面积保证和水域多形态编写生成测试。
- [x] 5.3 为事件最低配额、多实例稳定 ID、出生候选随机选择和配额失败诊断编写测试。
- [x] 5.4 对每主题运行 30 个固定种子，验证可重复哈希、四地貌出现、事件配额、资源零缺失和对象预算。
- [ ] 5.5 为每主题导出并人工审阅 3 个代表性种子的截图，检查地貌面积、形状、视觉层次和事件分散度。
- [ ] 5.6 统计地图加载时间、格网内存、视觉实例数和 GC 分配；修复超出项目性能预算的问题。
- [x] 5.7 更新 PCG 美术与运行时交接文档，使资源完成状态、配置作者流程和未来能力启用方式与实际项目一致。
