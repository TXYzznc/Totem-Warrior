# SelfTattooForm 线框文字稿（出图前确认）

> 基于 GDD `01-纹身构筑系统.md` §5.1 原始线框，结合本项目实际数据补充具体示例值。出图前请确认下列细节，确认后再生成效果图。

## 布局（居中覆盖层，约 50%×70% 屏幕）

```
纹身工作台                                    [×] (再按Tab关)
────────────────────────────────────────────────────
[人形示意图，6 槽：头/左臂/右臂/躯干/左腿/右腿]
  当前默认选中：右臂（已有空槽）
  选中部位：右臂 ▼     预估读条：5.0s

┌─ 颜色（库存数量示例）──────────────────────┐
│ ●红×3  ●黄×1  ○绿×0(灰)  ●蓝×2          │
│ ●紫×1  ○金×0(灰)  ○白×0(灰)            │
│ 当前选中：黄（金色描边）                    │
└──────────────────────────────────────────┘

┌─ 图案（解锁状态示例）──────────────────────┐
│ —直线✓  ○圆环✓  🌀螺旋🔒 ⚡闪电🔒 🔱锯齿🔒 │
│ ⭐星形🔒 〰️流线🔒 🐉兽形🔒                  │
│ 当前选中：直线（已解锁，金色描边）           │
└──────────────────────────────────────────┘

预览：右臂 × 黄 × 直线 = "黄电单点拳"

      [ 开始绘制（5.0s 读条）]   [取消]
```

## 待确认细节

1. **默认选中部位**：草稿用"右臂"（GDD 原例）。是否沿用，还是换成其他部位（如头部，读条最长 8s，更能体现风险）？
2. **库存数字示例**：草稿延用 GDD 原例（红×3/黄×1/绿×0/蓝×2/紫×1/金×0/白×0）。这只是效果图静态展示用的示例值，不影响实际数值表，是否沿用？
3. **图案解锁示例**：草稿延用"仅直线+圆环解锁，其余 6 个锁定"（对应游戏开局状态：初始配方 2 个）。是否沿用？
4. **预览文案**："黄电单点拳"是 GDD 原文示例的技能效果命名，效果图上只是占位文字，不代表正式数值/命名，是否保留？
5. **人形示意图风格**：用抽象简笔轮廓（圆点代表槽位），还是要更具体的角色侧影（类似 MainMenuForm/CharacterSelectForm 里那种写实厚涂风格）？
   - 倾向**抽象简笔**（GDD 原意是"功能性示意图"，类似装备槽位图，不需要写实角色）
6. **关闭方式**：图上是否需要画出右上角 [×] 按钮，还是像 ThreeChoiceForm 一样不画关闭按钮（因为本身可以再按 Tab 关闭，不强制选择，所以应该画 [×] 体现"可随时退出"）？
   - 倾向**画出 [×]**：自纹身不是强制流程，区别于 ThreeChoiceForm

## 确认结果

按草稿走：默认选中右臂 / 库存示例沿用 GDD 原例 / 仅直线+圆环解锁 / 保留"黄电单点拳"预览文案 / 抽象简笔人形示意图 / 画出右上角 [×] 关闭按钮。**移除风险提示文字**（不在效果图上画"风险：读条...扣50金币"那一行）。
