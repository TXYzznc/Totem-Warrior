# M0 只读基线（2026-08-11）

## 工程快照

- Git 分支：`main`
- Git HEAD：`9cc4498993871bfe93285e61de42b83a4507833a`
- 实现开始前工作树已存在大量用户/并行任务改动；重建得出约 122 个状态项（90 个 tracked、32 个 untracked）。本 change 不清理、不回滚这些改动。
- Unity：`2022.3.62f3c1`
- 渲染管线：URP `14.0.12`
- Test Framework：工程清单 `1.1.33`，当前 Library 实际解析到 `1.4.6`
- BuildSettings：`Launch.unity`、`TotemGame.unity`、`OasisCity.unity`
- Gameplay catalog SHA-256：`14333fddf637f3def46505c18c9a5f2e939448cda4ecbf4a424a719a8fc8aa1d`
- Runtime asset catalog SHA-256：`811fe339a4593b14019b6139f312b4aad090b6b6594182b51500cd1a8e71d010`
- 全量诊断报告：`GameData/Diagnostics/Reports/gf-diagnostics-run-all_20260811_101922.json`

## 全量诊断复跑

结果与讨论前保存的基线一致：`28 success / 9 failure / 36 warning`，没有数量差异。

| 失败项 | 直接证据 | 责任边界 |
|---|---|---|
| GF_X Rewrite Inventory Contract | 46 个系统资源未分类，45 个仍为 classification-needed | 资源索引/迁移收口；不由 M0 玩法修复硬清理 |
| Clean Workspace Contract | `Assets/Screenshots` 位置非法；`Assets/Resources` 超白名单 | 工作区治理；其中 PCG Resources 是当前地图依赖，迁移需单独决策 |
| Totem Enemy Domain Runtime | SpawnPlan 为空、Light 敌人未生成，随后空引用 | 本 change M0：地图—遭遇适配断裂 |
| Totem Extended Gameplay | 宝箱 30（期望 4），普通 24（期望 2），稀有 6（期望 2） | 旧诊断仍按旧地图配额；先记录，后按 First Playable 规格改写 |
| Totem Gameplay Runtime | PCG 地形类别、规范锚点、锚点/资源数量与旧断言不一致 | M0 修规范锚点与运行链；数量断言在地图验收规格确定后更新 |
| Totem Runtime Assets | `actor.player` Prefab 含 1 个 MonoBehaviour（期望 0） | 运行资源契约；需确认现有角色资源组件是否应保留 |
| Totem Runtime Catalog Binding | `ALIEN_HIVE` 遭遇没有生成 entry | 本 change M0：同一地图—遭遇适配断裂 |
| Totem Runtime Causality Smoke | 启动后无原生 Light 敌人、循环内无存活敌人 | 本 change M0：由 SpawnPlan 空导致 |
| Totem Runtime Object Pool | 没有敌人 visual，随后空引用 | 本 change M0：由 SpawnPlan 空导致 |

36 条 warning 全部属于同一类：Business Excel 时间戳新于 AI JSON，但导入仍使用 JSON。同步器报告 31 成功、0 失败、0 行/单元格变化；M0 记录为配置源时效警告，不把它误判成玩法故障。

## PlayMode smoke 复现

1. `TotemCombatHudInputSmokeTests.CombatHud_InputSmoke_UsesTotemInputService`：通过，约 23 秒，能够明确退出。
2. `TotemEnemyReadinessPcgSmokeTests.EnemyReadiness_DiagnosticFastPcgSmoke_CoversStartupAndImmediateParticipantCombat`：失败，能够明确退出，并非测试运行器死锁。
3. 最后成功阶段：`Launch -> MainMenu -> CharacterSelect -> StartupSelect -> CombatHUD`，本地玩家完成 Loading/Protected/Active 生命周期校验，参与者保护期校验完成。
4. 精确失败点：`TotemEnemyReadinessPcgSmokeTests.cs:72`，`EnemyWorld should spawn Light enemies from the authority clock plan`，实际 `lightCount = 0`。
5. 根因：PCG WorldPlan 的 `encounter`/`boss_spawn` 锚点把 `RegionId` 直接作为 `ZoneRole`；`TotemEncounterService` 只接受 `inner/mid/outer/danger/boss`，所以配置虽存在，锚点池仍为空。
6. Console 中另有 Oasis LookDev 自动流程和 Unity Inspector/CameraEditor 错误，来自实现开始前已存在的并行美术/编辑器改动；不修改其文件，作为外部噪声单独保留。

## M0 修复边界

- 只修复规范锚点 ID 与 PCG WorldPlan 到遭遇语义角色的适配。
- 不在 M0 引入六人队伍、三轮流程、枪械或纹身新玩法。
- 不以删除现有 PCG、美术资源或覆盖用户工作树来换取诊断变绿。

## 最小修复复验

- 修复后报告：`GameData/Diagnostics/Reports/gf-diagnostics-run-all_20260811_103235.json`
- 结果：`32 success / 5 failure / 36 warning`。
- 已关闭 4 个由同一根因造成的失败：Enemy Domain Runtime、Runtime Catalog Binding、Runtime Causality Smoke、Runtime Object Pool。
- Gameplay Runtime 不再报告四个规范锚点缺失；`player.spawn`、`boss.spawn`、`npc.tattooist.base`、`npc.merchant.base` 已恢复。
- 剩余 5 项分别属于资源分类、工作区治理、旧 PCG 数量/地形断言和 player Prefab 组件契约；没有为了凑全绿扩大 M0 玩法范围。
- 原始 EnemyReadiness DiagnosticFast PlayMode smoke 修复后 `1/1 passed`，约 20 秒并明确退出。退出竞态根因为待完成的 `TotemGameplaySceneLoader` 在返回主菜单后重新进入 CombatHUD；现在 `EnterMainMenu` 会取消待完成加载。

## M0 Gate 证据

- 编译：`debug_check_compilation` 返回 `isCompiling = false`、`isUpdating = false`，未发现编译错误。
- EditMode：`UnitySkills.Tests.Core.TestInfrastructureTests.TestGetResult_ExposesExtendedOutcomeCounters`，任务 `c0245116`，`1/1 passed`，约 7 秒并明确退出。
- PlayMode（核心链）：`TotemEnemyReadinessPcgSmokeTests.EnemyReadiness_DiagnosticFastPcgSmoke_CoversStartupAndImmediateParticipantCombat`，任务 `ada99223`，`1/1 passed`，约 20 秒并明确退出。
- PlayMode（合法出生抽样）：`TotemMapSpawnSelectionTests.RandomPlayerSpawnSelection_ChoosesOnlyFromAllValidCandidates`，任务 `9190f033`，`1/1 passed`，约 15 秒并明确退出。
- 结论：M0 的 7 项任务全部完成；剩余诊断失败已明确归属后续里程碑或工程治理，不阻断 M1 公共合同与数据骨架。
