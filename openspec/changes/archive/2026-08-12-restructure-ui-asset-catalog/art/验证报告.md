# UI 素材全量验证报告

验证日期：2026-07-27

## 范围与结果

对通用组件库和 8 个效果图对应的 `最终素材` 执行了逐文件检查：

| 项目 | 结果 |
| --- | --- |
| 最终 PNG 数量 | 58 |
| PNG 可打开 | 58 / 58 |
| 含 alpha 通道（`Argb` / `PArgb`） | 58 / 58 |
| 四角 alpha 均为 0 | 58 / 58 |
| 存在透明像素 | 58 / 58 |
| 文件大于 1 KiB | 58 / 58 |
| 清单、生成记录、验证报告齐备 | 9 / 9 目录 |
| 异常 | 0 |

## 验收口径

- 检查对象仅包含可交付的最终 PNG；`_source` 的纯绿色背景源图和 `_merged` 的去绿合并图不计入最终素材数。
- 最终文件的像素格式允许 `Format32bppArgb` 与等价的预乘 alpha 格式；仅按字符串包含 `Alpha` 判断会误报，因此已按实际像素格式复核。
- 文本、角色、纹身图案、物品图标与预览渲染内容不属于本次 PNG 交付范围，按各页面的 `素材清单.md` 在运行时注入。
