## 1. 现有系统与性能基线

- [ ] 1.1 核对 `TotemVfxService`、`TotemAssetService` 和现有对象池的实例化与回收契约，确定通用破坏 VFX 的唯一接入路径。
- [ ] 1.2 确定首期目标平台、最坏同屏触发次数以及 CPU/GPU/VFX 并发预算，并将阈值记录为可配置的运行时设置。
- [ ] 1.3 建立独立的视觉破坏验证场景，并以 `INT-PROP-009` 陶制储罐的原始模型和材质作为基线资产。

## 2. 共享渲染与 VFX 资源

- [ ] 2.1 创建 URP 兼容的通用视觉崩解 Shader，支持对象空间共享噪声、命中中心、裁切边缘、受限顶点外移和原有 Lit 外观保持。
- [ ] 2.2 创建共享噪声纹理、默认材质和陶瓷/木材/石材/金属的 Shader 参数配置，不制作单道具破损贴图或碎块模型。
- [ ] 2.3 创建可复用的陶瓷、木材、石材和金属碎屑/粉尘 VFX Profile，并限制单次发射数量、寿命和透明过度绘制。
- [ ] 2.4 将共享渲染与 VFX 资源登记到项目既有运行时资源目录和 asset catalog，禁止通过 `Resources.Load` 建立新加载路径。

## 3. 通用视觉破坏运行时能力

- [ ] 3.1 实现可挂载的静态道具视觉破坏组件，缓存 Renderer 与 MaterialPropertyBlock，并暴露材质类别、Effect Profile 和终态行为配置。
- [ ] 3.2 实现接收世界命中点、强度和完成通知的显式触发 API；确保组件不读取任何按键或输入动作，且播放期间幂等。
- [ ] 3.3 实现多 Renderer 的统一场次参数写入、对象空间命中点转换、一次性 VFX 发射与完成后的参数清理；确保 Update 路径无 GC 分配。
- [ ] 3.4 实现全局完整档、简化档和无 VFX 档令牌预算，并接入既有 VFX/对象池回收路径。
- [ ] 3.5 实现透明材质、Skinned Mesh、Terrain 和不兼容材质的 Inspector 校验与运行时安全降级。
- [ ] 3.6 实现终态 Renderer 隐藏、GameObject 禁用和所有者回调三种不销毁对象的生命周期行为。

## 4. 首个道具接入与可视化调校

- [ ] 4.1 为 `INT-PROP-009` 陶制储罐配置陶瓷视觉破坏组件与共享 Profile，确认无需破损模型、专属碎片或专属裂纹贴图。
- [ ] 4.2 在目标光照场景中调校陶罐的崩解时长、釉面边缘、顶点位移、碎屑和粉尘，使其保持陶瓷脆性而非果冻式膨胀。
- [ ] 4.3 验证低顶点模型、多 Renderer 模型和不支持材质的表现与降级路径。

## 5. 自动化验证与性能验收

- [ ] 5.1 添加 EditMode 测试，覆盖组件配置校验、重复触发抑制、材质类别选择和不支持 Renderer 的安全降级。
- [ ] 5.2 添加 PlayMode 测试，覆盖命中中心传播、三档预算选择、VFX 发射次数、完成回调和对象池/令牌释放。
- [ ] 5.3 在目标设备的最坏同屏触发场景中进行 Unity Profiler 采样，记录 CPU、GPU、GC 和透明过度绘制结果，并据此调整预算。
- [ ] 5.4 运行 GF_X 全量诊断，修复与本变更相关的编译、资源索引或运行时诊断问题。
