# Common Environment Modules — Completion Record

Status: complete (2026-07-16).

Eight neutral, low-frequency modules are available in `Assets/Game/Sprite/PCG/Props/Common/`: field lamp, shelter canopy, supply stack, signal beacon, barricade, cache nook, weapon rack, and choice plinth. Each is a 256px transparent PNG with validated `Default` / `Point` / `Clamp` / no mipmaps / `Uncompressed` import settings. They remain independent of theme anchor mappings so they cannot override existing themed interactions; attach them only through future room-semantic placement rules.
