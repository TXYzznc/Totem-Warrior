# UI 素材结构重制交付汇总

## 交付数量

| 范围 | 最终 PNG |
| --- | ---: |
| 通用 UI 组件 | 17 |
| UI-001 常驻 HUD | 7 |
| UI-002 纹身工作台 | 9 |
| UI-004 物资死亡箱撤离反馈 | 8 |
| UI-005 熟练总览 | 3 |
| UI-005 图案详情 | 3 |
| UI-005 构筑快照 | 8 |
| UI-005 战绩外观 | 2 |
| UI-005 解锁反馈 | 1 |
| **合计** | **58** |

## 复用与运行时边界

- 通用库集中提供面板、卡片、列表、按钮、焦点/选择、线性/圆环进度、状态、箭头和输入提示等 17 项；同形不同色统一使用 `Image.color` 或材质参数，不再导出颜色副本。
- 页面目录只保存无法由通用壳体表达的语义性壳体、遮罩和反馈标记。
- 所有普通文本由 `TMP_Text` 提供；输入提示在实现阶段由 InputModule 注入。
- 角色、纹身图案、颜料、物品、构筑内容、统计数值、3D/RT 预览均为运行时槽位或数据资产，不被烘焙成 UI Sprite。

## 旧产物清理状态

按用户选择“符合要求的保留，不符合要求的直接清理”，已清理 55 项不合格旧成品：UI-001 为 5 项、UI-002 为 20 项、UI-004 为 14 项、UI-005 图案详情为 16 项。四处目录均留有 `清理报告.md`。

UI-005 的熟练总览、构筑快照、战绩外观、解锁反馈仍有 59 项已审计的不合格旧成品待删除。删除已获用户授权，但当前执行环境拒绝所有删除命令；没有使用规避方式。待环境允许删除后，应以各自 `结构重制/素材清单.md` 中的“旧产物审计”精确列表为准处理，保留其中标记为可复用的项目与所有源图/文档。
