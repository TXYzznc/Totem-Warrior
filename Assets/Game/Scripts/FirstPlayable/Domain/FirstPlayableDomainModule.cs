namespace GameDesinger.FirstPlayable.Domain
{
    /// <summary>
    /// 新 First Playable 纯领域层的命名空间锚点。
    /// 领域类型只能依赖纯 C# 合同，不能依赖 Unity、数据加载、运行时服务或 UI。
    /// </summary>
    internal static class FirstPlayableDomainModule
    {
    }
}
