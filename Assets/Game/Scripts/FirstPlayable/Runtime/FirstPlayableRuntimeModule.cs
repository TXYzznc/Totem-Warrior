namespace GameDesinger.FirstPlayable.Runtime
{
    /// <summary>
    /// 新 First Playable 运行时编排层的命名空间锚点。
    /// 运行时层是 Domain 和 Data 到 GF_X 适配层之间的唯一编排入口。
    /// </summary>
    internal static class FirstPlayableRuntimeModule
    {
    }
}
